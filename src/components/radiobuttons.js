import React from 'react';
import { radio, Button } from 'antd';

const ButtonSize = () => {
   state = {
       size:"large"
   };
   handleSizeChange = e => {
       this.setState({size:e.target.value})
   }
}